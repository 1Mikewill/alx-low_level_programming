#include <stdio.h>
#include "main.h"

/**
 *main-> prints out name of my program
 *@argc: argument count
 *@argv: argument vector
 *Return: return always 0
 */

int main(int argc, char *argv[]);
{
	argc();
	printf("%s\n", argv[0]);

	return (0);
}
